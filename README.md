# Run and deploy

This contains everything you need to run your app locally.

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies in the directory you saved the project:
   `npm install`
2. Run the app:
   `npm run dev`
3. Open the app in the browser:
    'http://localhost:3000'
